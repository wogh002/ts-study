{
    // ts 설정파일
    "compilerOptions": {
        //allowJs?자바스크립트 파일 컴파일 허용 여부
        "allowJs": true,
        //target? tsc -> ts파일을 js 파일로 컴파일하는데 JS버전을 target 으로 설정
        "target": "ES5",
        //outDir? ts의 결과물이 어디로 들어갈 것인가.   
        "outDir": "./built",
        //moduleResolution? 프로미스 인식시켜주기 위해서
        "moduleResolution": "Node",
        "lib": [
            "es2015",
            "DOM",
            "DOM.Iterable"
        ],
        // noImplicitAny? true 일 경우 => 타입에 any 라도 붙혀라
        "noImplicitAny": true,
    },
    // include? 현재 프로젝트기준으로 어떤 파일들을 대상으로 typescript를 컴파일 시킬꺼냐?
    "include": [
        "./src/**/*"
    ],
}