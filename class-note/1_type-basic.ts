{
    // JS
    const str = 'hello';
    // TS 숫자 
    const name: string = 'jaeho';
    let weight: number = 200;
    //TS 배열
    let arr: Array<number> = [1, 2, 3];
    const heroes: Array<String> = ['capt', 'Thor'];
    let items: number[] = [1, 2, 3, 4, 5, 6, 7];

    // TS 튜플 (배열순서의 타입까지 정의하는것)
    const address: [string, number] = ['gananam', 123];

    // TS 객체
    let obj: object = {};
    let person: { name: string, age: number } = {
        name: 'thor',
        age: 10000
    }
    // TS 진위값
    const show: boolean = true;
}